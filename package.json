{
  "name": "game-pirates",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "lint": "yarn tslint --project tsconfig.json --format verbose",
    "test": "yarn mocha --recursive",
    "dev": "yarn watch 'yarn build' src lib img fonts map",
    "build": "yarn styles && yarn js && yarn img && yarn fonts",
    "styles": "yarn node-sass src/views -o build/styles && cat build/styles/**/styles.css > build/styles.css",
    "js": "yarn browserify -p common-shakeify -p browser-pack-flat src -o build/game.js",
    "debug:js": "yarn browserify -p common-shakeify --full-paths --debug src -o build/game-full.js",
    "debug:disc": "yarn discify build/game-full.js >build/disc.html",
    "debug:explore": "yarn source-map-explorer build/game-full.js build/game-full.js.map",
    "fonts": "cp fonts/retro-subset.woff2 build/",
    "img": "cp -R img build",
    "min:js": "yarn uglifyjs build/game.js -o build/game.min.js -c drop_console,ecma=7,keep_fargs=false,passes=2,pure_getters=true,unsafe_arrows=true -m --mangle-props regex=/^[A-Z_]+$/",
    "min:styles": "yarn csso build/styles.css build/styles.min.css --stat",
    "min:img": "bin/ect -9 -strip --allfilters-b -quiet build/img/*.png",
    "min:html": "yarn html-minifier build/index.html -o build/index.html",
    "min": "yarn build && yarn min:js && yarn min:styles && yarn min:img && yarn min:html",
    "zip": "rm -f build/game.zip && zip -9 -r build/game.zip build/game.min.js build/index.html build/retro-subset.woff2 build/styles.min.css build/img && bin/ect -9 -zip build/game.zip"
  },
  "dependencies": {},
  "devDependencies": {
    "browser-pack-flat": "^3.1.0",
    "browserify": "^16.2.2",
    "chai": "^4.1.2",
    "common-shakeify": "^0.5.1",
    "csso-cli": "^1.1.0",
    "disc": "^1.3.3",
    "exorcist": "^1.0.1",
    "html-minifier": "^3.5.19",
    "mocha": "^5.2.0",
    "node-sass": "^4.9.3",
    "source-map-explorer": "^1.6.0",
    "uglify-es": "^3.3.9",
    "watch": "^1.0.2"
  }
}
